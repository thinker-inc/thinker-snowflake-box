# ##################################
# ### external_table privileges(role に対して 指定した外部テーブルのprivilegesを付与する)
# ##################################
# external_table_privileges:
# ### ***********書式**********
#   - name: EXTERNAL_TABLE_PRIVILEGE # 一意な名前にする
#     role: role_name # String
#     class: SCHEMA OBJECT # String
#     type: EXTERNAL TABLES # String
#     parameter: # Object
#       database_name: db # String
#       schema_name: schema
#       privileges: # List(String) (SELECT | REFERENCES)
#         - SELECT
# ### ************************